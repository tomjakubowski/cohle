before_install:
  - yes | sudo add-apt-repository ppa:hansjorg/rust
  - sudo apt-get update
install:
  - sudo apt-get install rust-nightly
script:
  - rustc --version
  - rustc main.rs --test -o bin/test
  - bin/test
# language: rust # some day this will work...